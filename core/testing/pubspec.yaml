name: core_testing
publish_to: none
resolution: workspace
description: "Core testing helpers and mocks for Mamelog."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.1
  flutter: ^3.41.0

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4

dev_dependencies:
  flutter_lints: ^6.0.0
