name: core_common
publish_to: none
resolution: workspace
description: Common utilities and base classes for Mamelog.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.1

# Add regular dependencies here.
dependencies:
  logging: ^1.3.0
  meta: ^1.11.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
