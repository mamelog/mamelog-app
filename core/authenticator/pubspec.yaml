name: core_authenticator
publish_to: none
resolution: workspace
description: Authentication interface for Mamelog.
version: 1.0.0

environment:
  sdk: ^3.11.1

dependencies:
  core_common:
    path: ../common
  core_model:
    path: ../model
  freezed_annotation: ^3.1.0
  meta: ^1.16.0

dev_dependencies:
  build_runner: ^2.4.15
  freezed: ^3.2.0
  lints: ^6.0.0
  test: ^1.25.15
