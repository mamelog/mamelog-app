name: core_data
publish_to: none
resolution: workspace
description: Repository implementations for Mamelog.
version: 1.0.0

environment:
  sdk: ^3.11.1

dependencies:
  core_common:
    path: ../common
  core_database:
    path: ../database
  core_domain:
    path: ../domain
  core_model:
    path: ../model
  core_network:
    path: ../network
  injectable: ^2.7.1
  logging: ^1.3.0
  path: ^1.9.1
  path_provider: ^2.1.5
  rxdart: ^0.28.0
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.4.15
  injectable_generator: ^2.12.1
  lints: ^6.0.0
  mocktail: ^1.0.4
  test: ^1.25.15
