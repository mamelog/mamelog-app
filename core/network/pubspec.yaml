name: core_network
publish_to: none
resolution: workspace
description: Network client and API DTOs for Mamelog.
version: 1.0.0

environment:
  sdk: ^3.11.1

dependencies:
  core_authenticator:
    path: ../authenticator
  core_common:
    path: ../common
  core_model:
    path: ../model
  dio: ^5.8.0
  freezed_annotation: ^3.1.0
  injectable: ^2.7.1
  json_annotation: ^4.9.0
  meta: ^1.16.0
  retrofit: ^4.5.0

dev_dependencies:
  build_runner: ^2.4.15
  freezed: ^3.2.0
  injectable_generator: ^2.12.1
  json_serializable: ^6.9.3
  lints: ^6.0.0
  retrofit_generator: ^10.2.3
  test: ^1.25.15
