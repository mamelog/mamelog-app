name: core_model
publish_to: none
resolution: workspace
description: Domain models for Mamelog.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.1

# Add regular dependencies here.
dependencies:
  core_common:
    path: ../common
  freezed_annotation: ^3.1.0
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.4.0
  freezed: ^3.2.0
  lints: ^6.0.0
  test: ^1.25.0
